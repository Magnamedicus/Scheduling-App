// app/routes/home.tsx (or wherever your index route is)
import React from "react";
import Scheduler from "../components/scheduler";

export default function Home() {
  return (
      <main>
        <Scheduler />
      </main>
  );
}

